// cypress/support/e2e.js

// Import commands.js if you have custom commands
import './commands';

// Alternatively, you can add global hooks or configurations here
beforeEach(() => {
    // Code to run before each test
});

afterEach(() => {
    // Code to run after each test
});